# HF Space deploy — install medibot from the public GitHub repo + runtime deps.
git+https://github.com/lkshay/medibot.git@main

# Core runtime
gradio>=6.0
pandas>=2.0
pydantic>=2.0
python-dotenv>=1.0
httpx>=0.27

# Retrieval
sentence-transformers>=3.0
faiss-cpu>=1.8
numpy>=1.26

# LLM backends available in the Space
huggingface_hub>=0.25
google-genai>=0.3

# Guardrails (Presidio) — Note: spaCy model auto-downloaded at first call
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7

# Observability
langfuse>=4.0
